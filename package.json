{
  "name": "0-1-delphi-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "check": "npm run build && npm test",
    "scan": "tsx src/cli/scan.ts",
    "evaluate": "tsx src/cli/evaluate.ts",
    "history:archive": "tsx src/cli/archive-history.ts",
    "record:snapshot": "tsx src/cli/record-snapshot.ts",
    "replay": "tsx src/cli/replay.ts",
    "score:forecasts": "tsx src/cli/score-forecasts.ts"
  },
  "dependencies": {
    "@gensyn-ai/gensyn-delphi-sdk": "^2.1.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
